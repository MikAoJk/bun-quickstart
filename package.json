{
  "name": "bun-quickstart",
  "module": "index.tsx",
  "type": "module",
  "scripts": {
    "build": "bun build --target bun src/index.tsx --compile --outfile .build/index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}